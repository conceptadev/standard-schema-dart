/// Optional helpers for consuming Standard Schema issues.
///
/// Mirrors upstream's separate, optional `@standard-schema/utils` package. Kept
/// out of the main `standard_schema.dart` barrel so it stays opt-in: import it
/// explicitly with `import 'package:standard_schema/utils.dart';`.
library;

export 'src/utils.dart';
